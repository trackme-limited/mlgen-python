services:
  mlgen:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mlgen
    restart: unless-stopped
    env_file:
      - .env
    # Graceful shutdown timeout (allow queue drain)
    stop_grace_period: 30s
